server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Soporte para enrutado del lado del cliente
  location / {
    try_files $uri $uri/ /index.html;
  }

  # Página de error por defecto
  error_page 404 /index.html;
}